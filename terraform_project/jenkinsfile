pipeline {
    agent any

    tools {
        terraform 'terraform'
    }

    stages {
        stage('Terraform') {
            steps {
                sh 'terraform --version'
                sh 'terraform init'
                sh 'terraform validate'
                sh 'terraform plan'
            }
        }
    }
}
